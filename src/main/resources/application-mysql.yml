spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa-study?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: dnfntk1##
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
        highlight_sql: true
  h2:
    console:
      enabled: false
      path: /h2-console
logging:
  level.org.hibernate:
    orm.jdbc.bind: TRACE